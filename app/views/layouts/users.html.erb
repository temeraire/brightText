<!DOCTYPE html>
<html>
<head>
  <title>BrightText</title>
  <style>
    .flash {
      padding: 10px;
    }
    .notice {
      background: green;
    }
    .error {
      background: #FF5E5E;
    }
    .field_with_errors input {
      border: 1px solid #FF5E5E;
    }
    .field_with_errors label.message {
      color: red;
    }
  </style>
  <link rel="stylesheet" href="/stylesheets/reset.css">
  <link rel="stylesheet" href="/stylesheets/base.css">
  <link rel="stylesheet" href="/stylesheets/grids.css">
  <link rel="stylesheet" href="/stylesheets/fonts.css">
  <link rel="stylesheet" href="/stylesheets/skin.css">
</head>
<body>
  <% flash.each do |name, msg| %>
    <% if msg.is_a?(String) %>
      <div class="<%= name %> flash">
        <%= content_tag :div, msg %>
      </div>
    <% end %>
  <% end %>

  <div class="bg">
        <div class="mWrp">
            <div id="mHdr">
              <div class="logo">
                  <img src="images/logos/logo.png" alt="logo" />
                </div>
            </div>
            <div id="mCnt">
              <div class="cnt1">
                <%= yield %>
  <!--                   <h2>Login</h2>
                    <form action="#" method="get">
                        <ul class="login-form">
                            <li> <label>Email</label> <input type="text" class="txt" /> </li>
                            <li> <label style="margin:12px 0 2px;">Password</label> 
                            <input type="text" class="txt" /> </li>
                            <li> <span>Forgot Password?</span> <a href="#" class="btn">Login</a> </li>
                        </ul>
                    </form> -->
                </div>
            </div>
         </div>
    </div>

</body>