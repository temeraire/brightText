<% if session[:domain].name != "Advocacy"  %>
  <% breadcrumb :story_set, @story_set%>
<% end %>
<div class="col-md-10 col-md-offset-1">
  <h3>Editing <%= session[:style].set_alias.titleize %></h3>
  <%= render 'form' %>
</div>
